name: Makefile CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: write
env:
  ENV: base_raspberrypi_bcm27xx_bcm2709

jobs:
  build_base_raspberrypi_bcm27xx_bcm2709:
    runs-on: ubuntu-latest
    timeout-minutes: 480

    steps:
      - run: df -h
      # If brakes setup this: https://github.com/marketplace/actions/maximize-build-disk-space
      - name: "storage-cleanup"
        run: |
          sudo docker rmi $(docker image ls -aq) >/dev/null 2>&1 || true
          sudo rm -rf \
            /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup \
            /usr/lib/jvm || true
          echo "some directories deleted"
          sudo apt install aptitude -y >/dev/null 2>&1
          sudo aptitude purge aria2 ansible azure-cli shellcheck rpm xorriso zsync \
            esl-erlang firefox gfortran-8 gfortran-9 google-chrome-stable \
            google-cloud-sdk imagemagick \
            libmagickcore-dev libmagickwand-dev libmagic-dev ant ant-optional kubectl \
            mercurial apt-transport-https mono-complete libmysqlclient \
            unixodbc-dev yarn chrpath libssl-dev libxft-dev \
            libfreetype6 libfreetype6-dev libfontconfig1 libfontconfig1-dev \
            snmp pollinate libpq-dev postgresql-client powershell ruby-full \
            sphinxsearch subversion mongodb-org azure-cli microsoft-edge-stable \
            -y -f >/dev/null 2>&1
          sudo aptitude purge google-cloud-sdk -f -y >/dev/null 2>&1
          sudo aptitude purge microsoft-edge-stable -f -y >/dev/null 2>&1 || true
          sudo apt purge microsoft-edge-stable -f -y >/dev/null 2>&1 || true
          sudo aptitude purge '~n ^mysql' -f -y >/dev/null 2>&1
          sudo aptitude purge '~n ^php' -f -y >/dev/null 2>&1
          sudo aptitude purge '~n ^dotnet' -f -y >/dev/null 2>&1
          sudo apt-get autoremove -y >/dev/null 2>&1
          sudo apt-get autoclean -y >/dev/null 2>&1
          echo "some packages purged"
      - run: df -h

      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true # Initialize and update submodules

      - name: Cache OpenWrt Build
        uses: actions/cache@v4
        with:
          path: |
            openwrt/build_dir
          key: ${{ runner.os }}-openwrt-
          restore-keys: |
            ${{ runner.os }}-openwrt-


      # Ensure necessary directories exist and set permissions
      - name: Prepare OpenWrt directories
        run: |
          mkdir -p openwrt
          chmod -R 777 .
          

      - name: Make init
        run: |
          make init || make init -j1 V=s # Retry with single thread if the first attempt fails


      - name: Debug ChirpStack feed error log
        if: failure()    # <-- runs only if any previous step failed
        run: |
          if [ -f openwrt/logs/feeds/chirpstack/chirpstack/chirpstack/dump.txt ]; then
            echo "Found dump.txt, printing contents:"
            cat openwrt/logs/feeds/chirpstack/chirpstack/chirpstack/dump.txt
          else
            echo "No dump.txt found."
          fi
                    
      # Copy feeds.conf.default to the openwrt directory
      - name: Switching config to base_raspberrypi_bcm27xx_bcm2709
        run: |
          docker compose run --rm --user $(id -u):$(id -g) chirpstack-gateway-os make switch-env ENV=base_raspberrypi_bcm27xx_bcm2709

      - name: Permission fix cashed files 
        run: |
          docker compose run --rm --user $(id -u):$(id -g) chirpstack-gateway-os chown -R $(id -u):$(id -g) openwrt/build_dir
          docker compose run --rm --user $(id -u):$(id -g) chirpstack-gateway-os chmod 777 openwrt/build_dir
    
      - name: Fix TAR_OPTIONS for cashing
        run: |
          docker compose run --rm chirpstack-gateway-os bash -c "\
            grep -q -- '--owner=0' openwrt/include/download.mk \
            && sed -i 's/--owner=0 --group=0 //' openwrt/include/download.mk \
            || echo 'Already patched'"
      # # Copy feeds.conf.default to the openwrt directory
      # - name: Copy feeds.conf.default
      #   run: cp feeds.conf.default openwrt/feeds.conf.default
      - name: Build OpenWrt
        run: |
          docker compose run --rm chirpstack-gateway-os bash -c "cd openwrt && make defconfig -j$(nproc)"
          docker compose run --rm chirpstack-gateway-os bash -c "cd openwrt &&  make download -j$(nproc) V=s"
          docker compose run --rm chirpstack-gateway-os bash -c "make -j\$(nproc) V=s"


      # Ensure necessary directories exist and set permissions
      - name: Prepare OpenWrt for cache
        run: |
          sudo chmod -R 777 openwrt/build_dir
          

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: base_raspberrypi_bcm27xx_bcm2709
          path: openwrt/bin/targets/bcm27xx/bcm2709/chirpstack-gateway-os-*.gz

  build_full_raspberrypi_bcm27xx_bcm2709:
    runs-on: ubuntu-latest
    timeout-minutes: 480

    steps:
      - run: df -h
      # If brakes setup this: https://github.com/marketplace/actions/maximize-build-disk-space
      - name: "storage-cleanup"
        run: |
          sudo docker rmi $(docker image ls -aq) >/dev/null 2>&1 || true
          sudo rm -rf \
            /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup \
            /usr/lib/jvm || true
          echo "some directories deleted"
          sudo apt install aptitude -y >/dev/null 2>&1
          sudo aptitude purge aria2 ansible azure-cli shellcheck rpm xorriso zsync \
            esl-erlang firefox gfortran-8 gfortran-9 google-chrome-stable \
            google-cloud-sdk imagemagick \
            libmagickcore-dev libmagickwand-dev libmagic-dev ant ant-optional kubectl \
            mercurial apt-transport-https mono-complete libmysqlclient \
            unixodbc-dev yarn chrpath libssl-dev libxft-dev \
            libfreetype6 libfreetype6-dev libfontconfig1 libfontconfig1-dev \
            snmp pollinate libpq-dev postgresql-client powershell ruby-full \
            sphinxsearch subversion mongodb-org azure-cli microsoft-edge-stable \
            -y -f >/dev/null 2>&1
          sudo aptitude purge google-cloud-sdk -f -y >/dev/null 2>&1
          sudo aptitude purge microsoft-edge-stable -f -y >/dev/null 2>&1 || true
          sudo apt purge microsoft-edge-stable -f -y >/dev/null 2>&1 || true
          sudo aptitude purge '~n ^mysql' -f -y >/dev/null 2>&1
          sudo aptitude purge '~n ^php' -f -y >/dev/null 2>&1
          sudo aptitude purge '~n ^dotnet' -f -y >/dev/null 2>&1
          sudo apt-get autoremove -y >/dev/null 2>&1
          sudo apt-get autoclean -y >/dev/null 2>&1
          echo "some packages purged"
      - run: df -h

      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true # Initialize and update submodules

      - name: Cache OpenWrt Build
        uses: actions/cache@v4
        with:
          path: |
            openwrt/build_dir
          key: ${{ runner.os }}-openwrt-
          restore-keys: |
            ${{ runner.os }}-openwrt-


      # Ensure necessary directories exist and set permissions
      - name: Prepare OpenWrt directories
        run: |
          mkdir -p openwrt
          chmod -R 777 .
          

      - name: Make init
        run: |
          make init || make init -j1 V=s # Retry with single thread if the first attempt fails

      - name: Debug ChirpStack feed error log
        if: failure()    # <-- runs only if any previous step failed
        run: |
          if [ -f openwrt/logs/feeds/chirpstack/chirpstack/chirpstack/dump.txt ]; then
            echo "Found dump.txt, printing contents:"
            cat openwrt/logs/feeds/chirpstack/chirpstack/chirpstack/dump.txt
          else
            echo "No dump.txt found."
          fi
      # Copy feeds.conf.default to the openwrt directory
      - name: Switching config to full_raspberrypi_bcm27xx_bcm2709
        run: |
          docker compose run --rm --user $(id -u):$(id -g) chirpstack-gateway-os make switch-env ENV=full_raspberrypi_bcm27xx_bcm2709

      - name: Permission fix cashed files 
        run: |
          docker compose run --rm --user $(id -u):$(id -g) chirpstack-gateway-os chown -R $(id -u):$(id -g) openwrt/build_dir
          docker compose run --rm --user $(id -u):$(id -g) chirpstack-gateway-os chmod 777 openwrt/build_dir
      
      - name: Fix TAR_OPTIONS 4 cashing
        run: |
          docker compose run --rm chirpstack-gateway-os bash -c "\
            if grep -q '^TAR_OPTIONS' openwrt/include/download.mk; then \
              sed -i 's/^TAR_OPTIONS.*/TAR_OPTIONS += --no-same-owner --no-same-permissions/' openwrt/include/download.mk; \
            else \
              echo 'TAR_OPTIONS += --no-same-owner --no-same-permissions' >> openwrt/include/download.mk; \
            fi"
                    
      # # Copy feeds.conf.default to the openwrt directory
      # - name: Copy feeds.conf.default
      #   run: cp feeds.conf.default openwrt/feeds.conf.default
      - name: Build OpenWrt
        run: |
          docker compose run --rm chirpstack-gateway-os bash -c "cd openwrt && make defconfig -j$(nproc)"
          docker compose run --rm chirpstack-gateway-os bash -c "cd openwrt &&  make download -j$(nproc) V=s"
          docker compose run --rm chirpstack-gateway-os bash -c "make -j\$(nproc) V=s"


      # Ensure necessary directories exist and set permissions
      - name: Prepare OpenWrt for cache
        run: |
          sudo chmod -R 777 openwrt/build_dir          

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: full_raspberrypi_bcm27xx_bcm2709
          path: openwrt/bin/targets/bcm27xx/bcm2709/chirpstack-gateway-os-*.gz

